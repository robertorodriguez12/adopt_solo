<h1>Adopt Don't Shop</h1></b>
<h2>Shelters in the Denver Area</h2></b>
<%= link_to "All Pets", "/pets", method: :get %>
<%= link_to "All Shelters", "/shelters", method: :get %>
<% @shelters.each do |shelter| %>
  <%= link_to "#{shelter.name}", "/shelters/#{shelter.id}", method: :get %>
  <%= link_to "Edit Shelter", "/shelters/#{shelter.id}/edit", method: :get %>
  <%= link_to "Delete Shelter", "/shelters/#{shelter.id}", method: :delete %>
<% end %></b>
<h4><%= link_to 'New Shelter', '/shelters/new' %></h4>
